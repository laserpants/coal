module Main {

Main.message = "Hello, world! 🚀"

Main.main(_ : *) =
  let
    msg : string =
      Main.message : string
    in
      #(coal_print_string : string/*, msg : string)
      (fn(a : *) => 0)

}
