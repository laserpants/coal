module Main {

  import IO(__return_io)

  fun print(str) =
    #{coal_println_string : string -> IO<unit>}(str)(fn(_) => __return_io())

  fun main() =
    let 
      r = 
        { a = "hello" }
      in
        match(r) {
          | { a = str } => print(str)
        }
}
