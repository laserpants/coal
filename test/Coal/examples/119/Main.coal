module IO {

  print_string(str: string) : IO<unit> =
    #print_string(str)(fn(a) => ())

  fun main() =
    let 
      r = 
        { a = "hello" }
      in
        match(r) {
          | { a = str } =>
            println_string(str)
        }
}
