module Main {

  import IO(println_int32)
  import Eq(trait Equality(equal), type Answer(Yes, No))

  //import Eq(equal2, type Answer(Yes, No))

  fun main() =
    if (equal(Yes, No)) 
      then println_int32(1)
      else println_int32(222)

  // Two problems:
  //
  // 2. equal is not imported, which should also be an error

}
