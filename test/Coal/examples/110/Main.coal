module Main {

  type MyList<a> 
    = Nil 
    | Cons(a, MyList<a>)

  fold len : MyList<a> -> int32 {
    | Nil => 0
    | Cons(_, len(@next)) => next + 1
  }

  fun main() = 
    trace_int32(len(Cons("a", Nil)))

}
