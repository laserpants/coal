module Main {

  fun get_name2(p) = 
    match(p) {
      | ((name, _, _)) =>
          name
    };

  fun get_name((name, _, _)) = 
    name; 

  fun main() =
    trace_string(get_name2(("Lorenzo", 42, false)));

}
