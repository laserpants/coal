module Main {

  import IO(println_int32, println_bool, println_string, println_char)

  fun remove_name(r) =
    match(r) {
      | { name = x | q } =>
          q
    }

  fun main() =
    let
      q =
        remove_name({ name = "Tron", is_admin = true })
      in
        println_bool(q.is_admin)

}
