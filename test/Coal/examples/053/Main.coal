module Main {

  import IO(println_int32, println_bool, println_string, println_char)

  fun extend(r) =
    { name = "Wat" | r }

  fun length(xs) = 
    5 : int32

  fun main() =
    let
      q =
        extend({ baz = "hellO" })
      in
        let
          len =
            length([1, 2, 3 : int32])
          in
            println_string(q.name)

}
