module Main {

  type User = User({ uid : int32, name : string })

  a_user_named_bob = 
    User
      ({ name = "Bob"
       , uid = 1
       });

  fun get_user(user) = 
    match(user) {
      | User(o) =>
          o
    };

  fun main() =
    trace_string(get_user(a_user_named_bob).name);
}
