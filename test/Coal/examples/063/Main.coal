module Main {

  import Nat(pack, unpack)

  fun factorial(n : nat) =
    fold(n) {
      | Zero =>
          1
      | Succ(@p) as m =>
          m * p
    }

  fun main() =
    trace_int32(unpack(factorial(4)))

}
