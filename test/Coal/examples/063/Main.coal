module Main {

  import IO(println_int64, println_bool, println_string, println_char)
  import Nat(pack, unpack)

  fun factorial(n : nat) =
    fold(n) {
      | Zero =>
          1
      | Succ(@p) as m =>
          m * p
    }

  fun main() =
    println_int64(unpack(factorial(4)))

}
