module Main {

  import IO(println_string, println_int32, println_bool)

  fun faz(n : int32) =
    { baz = 
      { f = (fn(_) => n, "wat") 
      } 
    }

  fun main() =
    let 
      { baz = 
        { f = (a, b) 
        } 
      } = 
        faz(5)
    in
      println_int32(a(false))
}
