module Main {

  type List<a> 
    = Nil 
    | Cons(a, List<a>)

  fun len(lst) =
    fold(lst) {
      | Nil => 0
      | Cons(_, @next) => next + 1
    };

  cotype Stream<a> = { Head : a, Tail : Stream<a> }

  fun main() = 
    trace_int32(1);

}
