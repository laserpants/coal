module Main {

  fun main() =
    let 
      r = 
        { a = "hello", b = { msg = "hello from the other side" } }
      in
        match(r) {
          | { b = b1, a = _ } =>
              trace_string(b1.msg)
        };
}
