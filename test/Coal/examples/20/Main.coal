module Main {

  type User = User({ uid : int32, name : string })

  a_user_named_bob = 
    User
      ({ name = "Bob"
       , uid = 1
       });

  fun main() =
    match(a_user_named_bob) {
      | User(r) =>
          trace_string(r.name)
    };
}
