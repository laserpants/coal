module Main {

  import IO(println_int32, println_string)
  import String(is_prefix_of, length, drop)
  import Containers.Set(Set)
  import namespace Containers.Set
  import Coal.Combinators(snd)

//  fun process(seed : a, f : v -> a -> a) : Machine<a, v, a> =
//     process$_process(seed, f)
 
  type alias GameState = int32 // { a : int32 }

  let 
    aaa : List<GameState> =
      [5]
  
  //
//  let
//    foo : Process<GameState, unit> = 
//     let
//       step(_, x) = x
//     in
//       process(0, step)

//

  fun main() =
    println_int32(1)

}
