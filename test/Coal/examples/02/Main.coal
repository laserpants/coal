module Main {

  fun factorial(n : int32) : int32 =
    fold(pack_nat(n)) {
      | Zero =>
          1
      | Succ(@f) as m =>
          unpack_nat(m) * f
    };

  fun main() =
    trace_int32(factorial(4));

}
