module Main {

  fun factorial(n : int32) =
    fold(pack_nat(n)) {
      | Zero =>
          1
      | Succ(@p) as m =>
          unpack_nat(m) * p
    };

  fun main() =
    trace_int32(factorial(4));

}
