module Main {

  fun extend(r : { | t }) : { name : string | t } =
    { name = "Covfefe" | r }

  fun length(xs) = 
    5 : int32

  fun main() =
    let
      q =
        extend({ baz = "hellO" })
      in
        trace_string(q.name)

}
