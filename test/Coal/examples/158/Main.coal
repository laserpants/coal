module Main {

  import namespace IO
  import Json(type Foo)
  import Functor(map, trait Functor)

  let xs = map(fn(x) => "a" <> x, ["abc", "abcd"])

  fun main() =
    match(xs) {
      | h :: t => IO.println_string(h)
      | _ => IO.println_string("---")
    }

}
