module Main {

  import namespace IO
  import Json(Foo)
  import Coal.Functor(map, Functor)

  let xs = map(fn(x) => "a" <> x, ["abc", "abcd"])

  fun main() =
    match(xs) {
      | h :: t => IO.println_string(h)
      | _ => IO.println_string("---")
    }

}
