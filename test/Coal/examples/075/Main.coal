module Main {

  type MyList<a> 
    = Nil 
    | Cons(a, MyList<a>)

  fun len(lst) =
    fold(lst) {
      | Nil => 0
      // TODO: This should fail
      | @next => next + 1
    }

  fun main() = 
    trace_int32(len(Cons("a", Nil)))

}
