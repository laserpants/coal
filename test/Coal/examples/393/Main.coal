module Main {

  import namespace IO
  import IO(return)
  import MicroTest(Test, suite, run)
  import Data.Json.EncodingSpec(encodingSpec)
  import Data.Json.Encoding(encode_json_value)
  import Data.Json.Value(JsonValue)
  import String(double_to_string, intercalate)

  let
    spec : Test =
      suite([
        encodingSpec
      ])

  fun main() = // run(spec)
    let s =
      double_to_string(0.0)
//      encode_json_value(JsonNumber(0.0))
    in
    IO.println_string(s)

}
