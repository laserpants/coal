module Main {

  import IO(println_int32, println_bool, println_string, println_char)

  fun fst((a, b)) = a

  fun main() =
    let
      identity = 
        fn(x) => x
      in
        let p =
          (identity(3), identity("three"))
        in
          println_int32(fst(p))

}

