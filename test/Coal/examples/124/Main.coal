module Main {

  fun faz(n : int32) =
    { baz = 
      { f = (fn(_) => n, "wat") 
      } 
    }

  fun main() =
    let 
      { baz = 
        { f = (a, b) 
        } 
      } = 
        faz(5)
    in
      trace_int32(a(false))
}
