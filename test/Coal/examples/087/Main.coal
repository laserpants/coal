module Main {

  import IO(println_string, println_int32, println_bool)

  fun faz(n : int32) =
    { baz = 
      { f = (n, "wat") 
      } 
    }

  fun main() =
    let 
      { baz = 
        { f = (a, b) 
        } 
      } = 
        faz(4)
    in
      println_int32(a)
}
