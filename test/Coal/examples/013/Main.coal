module Main {

  import IO(println_int32)
  import Nat(pack, unpack)

  // Type error
  //
  fun factorial(n : int32) : int32 =
    fold(pack(n)) {
      | Zero =>
          1
      | Succ(@f) as m =>
          m * f
    }

  fun main() =
    println_int32(factorial(4))

}
