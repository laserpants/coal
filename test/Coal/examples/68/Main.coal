module Main {

  let baz =
    let
      print_name =
        fn(b) =>
          trace_string(b.name)
        in
          match({ name = "baz", id = "id" }) {
            | { name = name | t } =>
                print_name(t)
          };

  fun main() =
    trace_int32(1);

}
