module Main {

  import Nat(pack, unpack)
  import IO(println_int64)

  fun factorial(n : int64) =
    fold(pack(n)) {
      | Zero =>
          1
      | Succ(@p) as m =>
          unpack(m) * p
    }

  fun main() : IO<unit> =
    println_int64(factorial(4))

}
