module Main {

  import Nat(pack, unpack)

  fun factorial(n : int32) =
    fold(pack(n)) {
      | Zero =>
          1
      | Succ(@p) as m =>
          unpack(m) * p
    }

  fun main() : IO<unit> =
    trace_int32(factorial(4))

}
