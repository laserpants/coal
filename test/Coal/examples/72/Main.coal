module Main {

  fun remove_name(r) =
    match(r) {
      | { name = x | q } =>
          q
    };

  fun main() =
    let
      q =
        remove_name({ name = "Tron", is_admin = true })
      in
        trace_bool(q.is_admin);

}
