module Main {

  fun remove_name(r) =
    match(r) {
      | { name = _ | q } =>
          q
    };

  fun main() =
    let
      q =
        remove_name({ name = false, is_admin = true })
      in
        trace_bool(q.is_admin);

}
