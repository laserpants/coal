module Main {

  import Nat(pack, unpack)
  import IO(println_int64, println_bool, println_string)

  fun factorial(n) =
    fold(pack(n)) {
      | Zero =>
          1
      | Succ(@f) as m =>
          unpack(m) * f
    }

  fun main() =
    println_int64(factorial(8))

}
