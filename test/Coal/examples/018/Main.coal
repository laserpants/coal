module Main {

  import Nat(pack, unpack)

  fun factorial(n) =
    fold(pack(n)) {
      | Zero =>
          1
      | Succ(@f) as m =>
          unpack(m) * f
    }

  fun main() =
    trace_int32(factorial(8))

}
