module Main {

  trait X(a) {
    x : bool -> a
  }

  instance X(bool) {
    x = fn(z) => true;
  }

//  trait Ordered(a) {
//    compare : a -> a -> Ordering
//  }
//
//  instance Ordered(bool) {
//    compare =
//      fn(a, b) =>
//        EqualTo;
//  }

  fun main() = 
    let 
      a =
        x(false) // compare(false, true)
      in
        trace_bool(a);
}
