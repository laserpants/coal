module Main {

Main.main(_ : *) =
  let
    chr : char =
      '🎷'
    in
      let
        __call_tmp : * =
          @<*>
            ( coal_print_char : char/*
            , chr : char
            )
        in
          @<int32>
            ( fn(a : *) => 0
            , __call_tmp : *
            )

}
